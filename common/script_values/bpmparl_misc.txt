bpmparl_total_yea_votes_sv = {
    value = 0   
    if = {
        limit = {
            has_variable = bpmparl_total_yea_votes
        }
        add = var:bpmparl_total_yea_votes
    }
}

bpmparl_total_nay_votes_sv = {
    value = 0   
    if = {
        limit = {
            has_variable = bpmparl_total_nay_votes
        }
        add = var:bpmparl_total_nay_votes
    }
}

bpmparl_number_of_seats = {
    value = 0

    if = {
        limit = {
            exists = scope:test
        }
        add = scope:test
    }
    else = {
        add = 300
    }
}

bpmparl_number_of_seats_cached = {
    value = 0
    if = {
        limit = {
            has_variable = bpm_parliament_actual_number_of_seats
        }
        add = var:bpm_parliament_actual_number_of_seats
    }
}

bpmparl_test = {
    value = 0
    add = scope:test
    multiply = 15
}

